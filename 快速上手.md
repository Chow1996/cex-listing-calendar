# 快速上手指南

## 📦 需要上传的文件清单

### 核心文件（必须）
```
✅ index.html          - 网页主文件
✅ style.css           - 样式文件
✅ script.js           - JavaScript 逻辑
✅ scraper.py          - 爬虫程序
✅ vercel.json         - Vercel 配置
✅ .gitignore          - Git 忽略文件
✅ requirements.txt    - Python 依赖
✅ config.example.py   - 配置示例
```

### 数据文件（自动生成，首次可空）
```
📄 data.js             - 数据文件（由爬虫生成）
📄 cex_listings.json   - JSON 数据（由爬虫生成）
```

### 文档文件（可选）
```
📖 README_DEPLOY.md    - 部署说明
📖 文件清单.md         - 文件清单
📖 GIT操作指南.md      - Git 操作指南
📖 快速上手.md         - 本文件
```

### 脚本文件（可选）
```
🔧 update_and_push.sh  - 定时任务脚本
```

---

## 🚀 快速开始

### 第一步：准备 GitHub 仓库

1. 在 GitHub 创建新仓库：`cex-listing-calendar`
2. 不要初始化 README

### 第二步：初始化 Git（本地）

```bash
cd /Users/oker/cex-listing-calendar

# 初始化
git init

# 添加文件
git add index.html style.css script.js
git add scraper.py vercel.json .gitignore
git add requirements.txt config.example.py
git add README*.md 文件清单.md GIT操作指南.md 快速上手.md
git add update_and_push.sh

# 首次提交
git commit -m "Initial commit"

# 连接远程仓库（替换 yourusername）
git remote add origin https://github.com/yourusername/cex-listing-calendar.git

# 推送
git branch -M main
git push -u origin main
```

### 第三步：部署到 Vercel

1. 访问 https://vercel.com
2. 使用 GitHub 登录
3. 点击 "New Project"
4. 选择仓库 `cex-listing-calendar`
5. 配置：
   - Framework: Other
   - Root Directory: `./`
   - Build Command: （留空）
   - Output Directory: `./`
6. 点击 "Deploy"

### 第四步：设置 PythonAnywhere

1. 上传项目文件到 `/home/yourusername/cex-listing-calendar`
2. 安装依赖：`pip3.10 install --user telethon`
3. 创建 `config.py`（从 `config.example.py` 复制并填入 API 信息）
4. 首次运行爬虫：`python3.10 scraper.py`（需要登录 Telegram）

---

## 🔄 日常更新流程

### 1. 在 PythonAnywhere 运行爬虫
```bash
cd /home/yourusername/cex-listing-calendar
python3.10 scraper.py
```

### 2. 手动 Git 操作（本地或 PythonAnywhere）

```bash
# 下载更新的文件（如果在 PythonAnywhere 运行）
# 或直接在本地运行爬虫

# Git 操作
git add data.js cex_listings.json
git commit -m "Update: $(date +%Y-%m-%d)"
git push origin main
```

### 3. Vercel 自动部署
- Vercel 检测到 GitHub 更新
- 自动重新部署（1-2 分钟）

---

## ⚠️ 重要提示

1. **不要上传 `config.py`** - 包含 API 密钥
2. **不要上传 `telegram_session.session`** - Telegram 会话文件
3. **首次运行需要登录** - 运行 `scraper.py` 时会提示输入手机号和验证码

---

## 📝 文件说明

| 文件 | 说明 | 是否必须 |
|------|------|---------|
| `index.html` | 网页主文件 | ✅ 是 |
| `style.css` | 样式文件 | ✅ 是 |
| `script.js` | JavaScript 逻辑 | ✅ 是 |
| `data.js` | 数据文件（自动生成） | ✅ 是 |
| `scraper.py` | 爬虫程序 | ✅ 是 |
| `config.py` | 配置文件（**不要上传**） | ⚠️ 本地需要 |
| `vercel.json` | Vercel 配置 | ✅ 是 |
| `.gitignore` | Git 忽略文件 | ✅ 是 |

---

## 🆘 遇到问题？

查看详细文档：
- `README_DEPLOY.md` - 完整部署说明
- `GIT操作指南.md` - Git 操作详解
- `文件清单.md` - 完整文件清单

