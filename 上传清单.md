# 📤 GitHub 直接上传文件清单

## ✅ 需要上传的文件（按顺序）

### 1. 网页核心文件（必须）
```
index.html
style.css
script.js
data.js
```

### 2. 爬虫文件（必须）
```
scraper.py
config.example.py
requirements.txt
```

### 3. 配置文件（必须）
```
vercel.json
.gitignore
```

### 4. 文档文件（推荐）
```
README.md
快速上手.md
```

### 5. 脚本文件（可选）
```
update_and_push.sh
```

---

## ❌ 不要上传的文件

```
config.py                    (包含 API 密钥)
telegram_session.session     (Telegram 会话)
cex_listings.json           (可选，data.js 已包含)
__pycache__/                (Python 缓存)
.DS_Store                   (Mac 系统文件)
```

---

## 📝 上传步骤

1. **在 GitHub 创建新仓库**
   - 仓库名：`cex-listing-calendar`
   - 不要初始化 README

2. **上传文件**
   - 点击 "uploading an existing file"
   - 按上面的清单上传文件
   - 注意：`.gitignore` 是隐藏文件，需要显示隐藏文件才能看到

3. **提交**
   - Commit message: `Initial commit`
   - 点击 "Commit changes"

4. **上传后，在本地连接仓库**
   ```bash
   cd /Users/oker/cex-listing-calendar
   git init
   git remote add origin https://github.com/yourusername/cex-listing-calendar.git
   git pull origin main
   ```

---

## 🔧 首次运行爬虫

1. 创建 `config.py`（从 `config.example.py` 复制）
2. 填入 API_ID 和 API_HASH
3. 运行：`python3 scraper.py`
4. 会生成 `data.js` 和 `cex_listings.json`

---

## 🚀 部署到 Vercel

上传到 GitHub 后：
1. 访问 vercel.com
2. 导入 GitHub 仓库
3. 自动部署

